algorithm main(output int8 led)
{
/*
  $display("hello world",a,b,c);

  if (!r0 && !r1 && !r2 && !r3 && !r4 && !r5 && !r6) {
  }

  if (a === b) {    
  } else { if (a == b) {    
  } }

  if (a !== b) {    
  } else { if (a != b) {    
  } }

  b = !b ;
  c = a ~ b;
  c = a ^ b;
  c = a ^~ b;
  c = a ~^ b;
  c = ~ b; 
  c = a ~ ~ b;

  if (a && b || c) {  
  }

  c = a & 3;
  
  a = &a;
  a = |a;
  a = ~|a;
  a = ~&a;
  a = ~^a;
  a = ^~a;
  
  b = b << 2;
  b = b <<< 3;
  b = b >> 2;
  b = b >>> 3;
  
  a = {1b1,3b101,2hFF} + 53;
*/

    done:
  goto done;

 done2 :
  goto done2;

  switch (a) {
    case 0: {
	
	}
    case 1: {
	
	}
	case 2: {
	
	}
	default: {
	
	}
  }

}
