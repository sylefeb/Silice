algorithm main(output int8 leds)
{

  uint28 counter = 0;       // a 28 bits unsigned integer
  leds := counter[20,8];    // LEDs track the 8 most significant bits
  while (counter != 255) {  // while (1), limited for unit testing
    counter = counter + 1;  // count
  }

}
