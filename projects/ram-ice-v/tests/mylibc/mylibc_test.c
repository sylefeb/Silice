#include "mylibc.h"

void print_dec(int val);

void main()
{
/*  
    for (int j = 0 ; j < 200 ; j++) {
      for (int i = 0 ; i < 320 ; i++) {
        *(FRAMEBUFFER + i + (j << 9)) = (unsigned char)(i);
      }
    }
*/
 //printf("Hello world\nbouh");
 printf("\nABC %d",54321);
 
 asm("unimp\n");

}
