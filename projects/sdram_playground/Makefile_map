
.DEFAULT: sdram_ctrl_cpu_map.si
		silice-make.sh -s sdram_ctrl_cpu_map.si -b $@ -p basic,sdram -o BUILD_$(subst :,_,$@) $(ARGS)

icarus: sdram_ctrl_cpu_map.si
		silice-make.sh -s sdram_ctrl_cpu_map.si -b $@ -p basic -o BUILD_$(subst :,_,$@)

clean:
	rm -rf BUILD_*
